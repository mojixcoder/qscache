name: Running tests

on: 
  push:
  pull_request:
    branches:
      - main

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Running tests
        uses: actions/checkout@v2
        run: |
          cd qscache/
          pip install -r requirements.txt
          python3 manage.py test
